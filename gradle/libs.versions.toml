[versions]
capsule = "0.0.1"
readme = "0.0.0"

[libraries]

[plugins]
capsule = { id = "com.cheroliv.capsule", version.ref = "capsule" }
readme = { id = "com.cheroliv.readme", version.ref = "readme" }

[bundles]
