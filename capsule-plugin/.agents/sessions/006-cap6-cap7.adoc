= Session 006 — CAP-6 Integrations N3 + CAP-7 capsuleDistrib FFmpeg
:type: dev
:date: 2026-05-16
:score: 10/10

== Résumé

Session livrée avec 36/36 tests PASS (25 unit + 6 functional + 5 Cucumber).
Deux nouvelles tâches Gradle ajoutées au pipeline capsule.

== CAP-6 — capsuleCompositeContext (Integrations N3)

Tâche `collectCapsuleContext` qui exporte le contexte des capsules
dans un fichier JSON compatible avec le moteur engine N3.

Fichier modifié : `src/main/kotlin/com/cheroliv/capsule/CapsuleManager.kt`
- Classe `CapsuleCompositeContextTask` (~80 lignes)
- JSON builder manuel (pas de dépendance kotlinx.serialization)
- Format JSON : `{ source, version, entries[], timestamp }`
- Chaque entrée : deckName, slideCount, originalVideo, distribVideo, viewport, distribDimensions, slides[], ttsEngine, ttsVoice

3 tests unitaires (CapsuleCompositeContextTaskTest) :
- Création du fichier JSON context
- Support multi-deck
- Inclusion du chemin distribVideo si disponible

== CAP-7 — capsuleDistrib (FFmpeg recadrage 9:16)

Tâche `deployCapsule` qui recadre capsule.webm en format vertical 9:16
pour TikTok/Shorts/Reels, avec fallback copy si FFmpeg absent.

Fichier modifié : `src/main/kotlin/com/cheroliv/capsule/CapsuleManager.kt`
- Classe `CapsuleDistribTask` (~90 lignes)
- FFmpeg : scale+force_original_aspect_ratio=increase puis crop
- Dimensions paramétrables dans CapsuleExtension (1080x1920 par défaut)
- Multi-deck : un output par deck
- Fallback : copie as-is si ffmpeg indisponible

3 tests unitaires (CapsuleDistribTaskTest) :
- Warning si aucune vidéo trouvée
- Copie videos quand ffmpeg absent
- Support videos multiples

== CapsuleExtension — nouveaux paramètres

Fichier modifié : `src/main/kotlin/com/cheroliv/capsule/CapsuleModels.kt`

- `ffmpegExecutablePath` : chemin ffmpeg (défaut "ffmpeg")
- `distribOutputWidth` : largeur sortie (défaut 1080)
- `distribOutputHeight` : hauteur sortie (défaut 1920)
- `compositeContextOutputFile` : chemin fichier JSON (défaut "capsule/capsule-context.json")

== Tests fonctionnels

2 tests fonctionnels ajoutés (CapsulePluginFunctionalTest.kt) :
- `can run deployCapsule task`
- `can run collectCapsuleContext task`

== Règles respectées

- ✅ Règle 1 : Aucun commit
- ✅ Règle 2 : Aucun publish
- ✅ Règle 3 : DAG N2 respecté
- ✅ Règle 6 : Zéro secret en dur
- ✅ Règle 7 : CAPSULE CONSUME SLIDER (lecture seule)

== Pipeline final (rappel)

  AsciiDoc → slider-gradle → deck.html + *-script.txt
  → generateCapsuleScript → generateCapsule (TTS)

  → generateCapsuleVideo (Playwright capture → .webm)

  → deployCapsule (FFmpeg crop 9:16)

  → collectCapsuleContext (JSON → engine N3)
